/**
 * UserController
 *
 * @description :: Server-side actions for handling incoming requests.
 * @help        :: See https://sailsjs.com/docs/concepts/actions
 */

module.exports = {
  

  /**
   * `UserController.fullName()`
   */
  fullName: async function (req, res) {
    return res.json({
      todo: 'fullName() is not implemented yet!'
    });
  },

  /**
   * `UserController.emailAddress()`
   */
  emailAddress: async function (req, res) {
    return res.json({
      todo: 'emailAddress() is not implemented yet!'
    });
  },

  /**
   * `UserController.password()`
   */
  password: async function (req, res) {
    return res.json({
      todo: 'password() is not implemented yet!'
    });
  }

};

